include _mixins
doctype html
html(lang="ru")
    head
        include _head

    body(class=bodyClass)
        
        #grid-desktop
        #grid-tablet
        #grid-mobile

        .grid-buttons
            .js-desktop Grid Desktop
            .js-tablet Grid Tablet
            .js-mobile Grid Mobile

        .btn.btn--chat Задать вопрос

        
        .page(class=outClass)
            .page__content

                block header
                    include ../components/_header
                block content

                block footer
                    include ../components/_footer
                block scripts
                    include _scripts
